# Rust
/target
**/*.rs.bk
Cargo.lock.bak

# Editors / OS
.DS_Store
.idea/
.vscode/
*.swp
*~

# Loom local config (created if/when Loom is installed)

# Build artifacts
*.log
# Committed benchmark evidence log (recovered at-scale run; see issue #556)
!benchmarks/transmon_bench_cpu/geode_runs_1p16M_2026-07-15.log
# Committed Palace run evidence log (tracked before *.log rule; negated for consistency)
!reference/fixtures/transmon_palace/results_p1/palace_run_v22.log
.loom/daemon-state.json
.loom/[0-9][0-9]-daemon-state.json
.loom/stuck-history.json
.loom/alerts.json
.loom/health-metrics.json
.loom/progress/

# Python (reference/ implementations)
__pycache__/
*.pyc
*.pyo
.python-version
venv/
.venv/
.venv-jax/
.venv-*/

# Java / Maven (reference/tf_java/ build outputs)
**/target/
**/.classpath
**/.project
**/.settings/

# Visualization artifacts (geode_viz outputs — issue #277, Epic #276)
# Generated PNG / SVG plots from tools/viz/ never land in version control;
# regenerate locally with `python -m geode_viz.plots.<benchmark>`.
artifacts/

# Python build artifacts (geode_viz package)
*.egg-info/
build/
dist/
# Claude Code agent-isolation worktrees (local session state, never committed)
.claude/worktrees/

# Local credentials / secrets — never commit
.env
.env.*
!.env.example
*.local.md
/.voice/

# Repo Skills machine-local install metadata (absolute source path + timestamp)
.claude/skills/repo/.install-local.json
# >>> loom-managed (do not edit) >>>
# Loom runtime state (don't commit these)
.loom-in-use
.loom-checkpoint
.loom-managed
.no-changes-needed
.loom/.daemon.pid
.loom/.daemon.log
.loom/daemon.sock
.loom/daemon-loop.pid
.loom/daemon-metrics.json
.loom/loom-source-path
.loom/spawn-loop-state.json
.loom/spawn-loop.pid
.loom/stop-spawn-loop
.loom/issue-failures.json
.loom/interventions/
.loom/worktrees/
.loom/worktrees-local/
.claude/worktrees/
.loom/state.json
.loom/mcp-command.json
.loom/activity.db
.loom/claims/
.loom/locks/
.loom/signals/
.loom/status/
.loom/retry-state/
.loom/exit-codes/
.loom/sweep-checkpoint/
.loom/sweep-run/
.loom/stats/
.loom/diagnostics/
.loom/guide-docs-state.json
.loom/metrics_state.json
.loom/manifest.json
.loom/stuck-config.json
.loom/metrics/
.loom/usage-cache.json
.loom/claude-config/
.loom/tokens/
.loom/accounts.env
.loom/account-health.json
.loom/account-health.lock
.loom/CANARY
.loom/*.log
.loom/*.sock
.loom/*.tmp
.loom/*.bak
.loom/logs/
# <<< loom-managed <<<

# Anvil paper-vision per-page render intermediates (regenerable at 200 DPI from main.pdf)
papers/**/*.vision/pages/
**/*.vision/pages/
**/*.vision/slides/

# Vendored Anvil skill install — reinstall with anvil's install.sh.
# Untracked deliberately: a copy-installed tool tree does not belong in a
# public repo, and this one carried third-party content (2AMLogic/2am#94).
.anvil/
